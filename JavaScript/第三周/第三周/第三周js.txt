作业内容：

一：

创建一个页面

提示用户输入名字

对用户的输入进行输出

要求：提交页面文件

二：

2.1	定义变量，给其赋值一个数字。使用显式转换，将这个变量内的数字转换为字符串。

2.2	定义变量，给其赋值一个字符串，使用显式转换，将这个变量内的字符串转换为数字。

2.3	定义变量，给其赋值一个字符串或数字，使用显式转换，将这个变量内的字符串或数字，转换为布尔型数据。

要求： 提交浏览器运行的截图，保存到一个word文件中。

三：

创建一个页面

在页面中编写代码完成功能

使用if……else语句，当输入为1时输出“yes”，其他输入输出“no”

要求：提交页面文件



